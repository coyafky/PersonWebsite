# Components

Reusable UI components belong here.

Expected first components:

- Site navigation
- Page header
- Article list item
- Project card
- Tag list
- Markdown/MDX content wrappers

Keep components small and focused. Avoid placing content parsing logic here; content loading belongs in `lib/content/`.
