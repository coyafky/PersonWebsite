# Public Assets

Static assets belong here.

Expected assets:

- Resume PDF
- Project screenshots
- Open Graph images
- Favicons

Use stable filenames for assets referenced from Markdown or MDX.

Blog body images live under `images/blog/<post-slug>/`. Use `npm run images:add` instead of copying them into an ad-hoc directory; see `docs/agent/blog-image-workflow.md`.
